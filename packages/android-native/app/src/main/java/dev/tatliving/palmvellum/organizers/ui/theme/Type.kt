package dev.tatliving.palmvellum.organizers.ui.theme

import androidx.compose.material3.Typography

// Palm OS used Helvetica; Android's platform sans-serif (Roboto) is the
// always-available match and what the PWA falls back to. The Material3
// default Typography already uses the platform sans, so we keep it.
val PalmTypography: Typography = Typography()
