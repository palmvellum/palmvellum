{
  "name": "@palmvellum/android",
  "version": "0.1.0-pre",
  "private": true,
  "description": "Palm Organizers — Android companion app for the PalmVellum platform (Capacitor wrapper around the SvelteKit PWA).",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:web": "PUBLIC_BASE_PATH='' PUBLIC_RUNTIME=capacitor pnpm --filter @palmvellum/pwa build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.1.2",
    "@capacitor/core": "^7.0.0",
    "@capacitor/network": "^7.0.4",
    "@capacitor/preferences": "^7.0.4",
    "@capacitor/status-bar": "^7.0.6"
  }
}
