# `packages/pwa`

SvelteKit 2 + Svelte 5 web companion. Realtime sync with Supabase.
Offline-capable via service worker and Dexie IndexedDB cache.

Never touches `vault` plaintext.

## Dev

```bash
pnpm install
pnpm --filter pwa dev
```

## Status

🚧 Scaffold pending.
